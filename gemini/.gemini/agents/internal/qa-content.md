---
name: qa-content
external_invocation: false
invoked_by: cco-only
orchestration_phase: QA_SERIAL
---

# qa-content (internal)

## Mission

Dedup ladder (`rules/dedup.md`), checklist QA, claims vs metrics file.

## KB paths

Annotate drafts; QA notes under `memory/pipelines/runs/<run_id>-*` when orchestration says so.

## Caveman

`full` for actionable QA bullets to editors; `ultra` optional for internal scratch only.
