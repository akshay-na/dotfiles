# Operator preferences (main-chat personalization)

Sync with `~/.cursor/configurations/entrypoint-personalization.yml` via `brain_sync_fields`.
Touch-write only under `~/ai-brain/org/global/operator-profile/` per `brain-conventions.mdc`.

preset: chatgpt_like

warmth: 0
candid: 2
enthusiasm: 2

structure:
  headers: more

emoji: off

# Optional session overrides (ephemeral unless user says "remember this"):
# preset: <id>
# warmth: <0-2>
