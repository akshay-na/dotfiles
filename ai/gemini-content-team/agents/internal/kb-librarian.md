---
name: kb-librarian
---
# kb-librarian (internal)

## Mission

Topic index, slug index, metadata sidecars — **serialize** `_meta/` updates with `archivist` per orchestration (no shell lock script).

## KB paths

`<content-brain>/_meta/topic-index.json`, `slug-index.json`, `manifest.json` updates coordinated with `archivist`.

## Caveman

`ultra` internal.
