---
description: "How org-level and project-level agents coordinate, escalate, and hand off work"
alwaysApply: true
---

# Agent orchestration and escalation

- **Start with `cto` in plan mode** when a task has multiple steps, non-trivial trade-offs, or touches architecture, security, performance, or observability.
- **Use `senior-dev` as the default implementation agent** for any code or configuration change, unless the user explicitly requests a different agent or the work clearly requires a `cto`-driven multi-phase plan first.
- **Call `senior-dev` directly** for genuinely small, localized tasks (single-file fixes, trivial config tweaks) where a full plan would be overkill, and as the primary executor for plan-driven work once the `cto` has produced an approved plan.
- **Use `vp-onboarding`** to bootstrap or refresh project-local agents, rules, and skills in a repo's `.cursor/` directory; never modify org-level agents from project agents.
- **Default modes:** `cto` and other org VPs run in plan mode; `senior-dev` and all project-level `tech-lead`, `dev-*`, `sme-*`, and `qa` agents run in Agent (implementation) mode by default and only escalate to `cto` for a new plan when they discover architectural or multi-phase scope.

- **Org vs project responsibilities**
  - Org agents (`cto`, `vp-architecture`, `vp-engineering`, `ciso`, `sre-lead`, `staff-engineer`, `vp-platform`, `senior-dev`) own cross-project strategy, plans, and high-level standards.
  - Project agents (`tech-lead`, `dev-*`, `sme-*`, `qa`, `devops`) own implementation inside a specific repo, following org standards and project rules.
  - Project agents escalate up to org agents via `cto` when decisions affect architecture, security posture, performance budgets, or shared tooling.

- **Memory access**
  - All agents access memory directly via the `context-memory` skill and the always-apply `memory-access` and `memory-capture` rules. No broker agent is needed.
  - Agents follow the skill's read protocol (check directory → read `_index.md` → read individual entries) to avoid context bloat.
  - Agents follow the skill's write protocol (with mandatory `_index.md` update) to persist decisions, constraints, risks, and other entries.
  - The `memory-capture` rule instructs all agents to auto-capture significant decisions as part of their normal workflow.
  - Memory content is local per machine; it does not sync via dotfiles.

- **Docs orchestration**
  - The org-level `docs-researcher` agent is the **only** agent that may call documentation MCPs directly (e.g. Context7 documentation-lookup, HTTP fetch/WebFetch for docs, `rovo` for Confluence/Jira docs).
  - When an agent needs documentation or web research, it should:
    - Summarize the question and relevant technologies/APIs.
    - Delegate to `docs-researcher` as a subagent instead of calling documentation MCPs itself.
  - Agents should keep docs requests **coarse-grained and focused** (for example, "summarize official guidance for X in version Y" or "compare options A vs B from the vendor docs") and rely on `docs-researcher` for:
    - Distilled answers.
    - Key caveats.
    - Links/citations for deeper reading.
  - It is acceptable to run multiple `docs-researcher` subagents in parallel for independent topics, but each call should stay small and targeted to avoid bloating context.

- **Call-chain boundaries**
  - For multi-phase work: the user → `cto` (plan) → project `tech-lead` (execution orchestration) → `dev-*` / `sme-*` / `qa` (task execution). Org specialists (`vp-architecture`, `ciso`, etc.) are invoked only by `cto` in plan mode, not by project agents.
  - `tech-lead` is the single entrypoint for project execution. The user should avoid invoking `dev-*`, `sme-*`, or `qa` directly for plan-driven work; instead, call `tech-lead`, who decides which project agents to involve.

- **When to escalate to specialists (via `cto`)**
  - `vp-architecture`: new services, significant refactors, data-model or boundary changes, cross-service contracts.
  - `ciso`: auth flows, secret management, input validation on public endpoints, CI/CD or container security.
  - `vp-engineering`: concurrency, retries, connection pools, queueing, performance regressions on hot paths.
  - `sre-lead`: logging, metrics, tracing, health checks, rollout/rollback strategy, SLO/SLA questions.
  - `staff-engineer`: large refactors, de-tangling complex modules, naming/structure changes with wide impact.
  - `vp-platform`: repeated patterns that suggest templates, CLIs, generators, or shared libraries.

- **Plan and phase discipline**
  - In plan mode, `cto` groups work into numbered phases with explicit verification and rollback for each phase.
  - Implementation agents (usually `senior-dev` or project `tech-lead`/`dev-*`) must:
    - Follow phases in order.
    - Run the specified verification for each phase before claiming it complete.
    - Stop and wait for explicit user approval at every checkpoint before moving to the next phase.

- **User approval semantics**
  - "Explicit user approval" at a checkpoint means the user uses the approval wording in the plan (for example replying with **"proceed"** as instructed) or an equally clear statement that the next phase may begin.
  - Silence, unrelated conversation, side questions, or generic praise must **never** be treated as approval to move to the next phase by `senior-dev`, `tech-lead`, `dev-*`, `sme-*`, or `qa` agents.

- **Division of labor during implementation**
  - `cto` and VPs: design the plan and define phases, risks, and verification.
  - `senior-dev` / project `dev-*`: implement the plan with minimal diffs, matching existing patterns.
  - `tech-lead`: maps plan phases to project agents, tracks progress, and enforces checkpoints inside the repo.
  - Specialists (`ciso`, `sre-lead`, etc.): review or extend the plan/implementation only inside their domain.

- **Golden rule**
  - Every agent respects its scope, escalates instead of guessing outside that scope, and never skips plan checkpoints without explicit user instruction.
  - Every agent keeps its own context lean by following the `context-memory` skill for memory operations instead of loading heavy skills and rules into every agent.

## Project team structure and discovery

- Projects use typed team member names created by `vp-onboarding`:
  - `tech-lead` as the single execution entrypoint.
  - `dev-<scope>` for developer roles (e.g. `dev-frontend`, `dev-backend`, `dev-infra`).
  - `sme-<domain>` for subject-matter experts.
  - `qa-<scope>` for quality-focused roles (e.g. `qa-unit`, `qa-e2e`).
  - `devops` for CI/CD and infra when warranted.
- `tech-lead` must always **discover the current team** before delegating work (both sequentially and in parallel) by:
  - Listing `.cursor/agents/` files that match team patterns (`dev-*`, `sme-*`, `qa-*`, `devops`).
  - Reading each agent's stated scope and building an internal `| Agent | Scope |` table for assignment.
  - Re-running discovery at the start of each phase or when onboarding changes the team.
- All project agents must keep context minimal:
  - Load only the files, rules, and skills needed for the current task.
  - Access memory via the `context-memory` skill and delegate docs lookups to `docs-researcher` instead of loading large histories or whole documentation sets.
